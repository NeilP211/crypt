[package]
name = "crypt-proto"
version = "0.1.0"
description = "gRPC service and message definitions for the Crypt search API"
edition.workspace = true
license.workspace = true

[dependencies]
prost = "0.13"
tonic = "0.12"

[build-dependencies]
tonic-build = "0.12"
